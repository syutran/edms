<%= form_for @photo, url: {action: "create"}, :html => {:multipart => true, "data-ajax" => false} do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <fieldset>
    <%= f.hidden_field :device_id %>
    <%= f.hidden_field :user_id %>
    <div class="field">
    <div><%= f.label :image, "上传图片" %></div>
    <div><%= f.file_field :image %></div>
  </div>
  <div class="field">
    <%= f.label :explain, "图片说明" %>
    <%= f.text_area :explain %>
  </div>
  <div class="actions">
    <%= f.submit "确认提交" %>
  </div>
</fieldset>
<% end %>
